Immutability:
-an immutable object is an object whose state cannot be modified after it is created
-number and strings are immutable
-objects, arrays, functions, classes, sets and maps are mutable

Predictability:
-do away with side-effects

Mutation Tracking:
-provide the ability to see all changes to our application over time

Redux and Immutability:
-large applications